{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace wk-leeches build",
    "dev": "yarn workspace wk-leeches dev",
    "build:watch": "wsrun -p @chooban/leeches -r build:watch",
    "build:browser:watch": "wsrun -p wk-leeches -r build:watch"
  },
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "dotenv": "^8.0.0",
    "typescript": "^3.5.3",
    "wsrun": "^5.0.0"
  },
  "dependencies": {
    "eslint-config-prettier": "^6.0.0",
    "eslint-plugin-prettier": "^3.1.0",
    "prettier": "^1.18.2"
  }
}
